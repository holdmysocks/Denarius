# Denarius v1.1.0
#
# DENARIUS_VERSION tags the images built below. Keep it in step with the
# version in frontend/package.json, backend/app/main.py and README.md when
# cutting a release. Override per-deploy with DENARIUS_VERSION in .env.
#
# Note: there is deliberately no top-level `version:` key — that field is the
# Compose file-format version, which is obsolete in Compose v2 and only emits
# a deprecation warning.

services:
  # Runs once on first launch to generate POSTGRES_PASSWORD and JWT_SECRET.
  # Secrets are written to the denarius_secrets volume and reused on subsequent restarts.
  config-init:
    image: alpine:3.19
    restart: "no"
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-}
      JWT_SECRET: ${JWT_SECRET:-}
    volumes:
      - denarius_secrets:/secrets
    command: |
      sh -c '
      if [ ! -f /secrets/secrets.env ]; then
        echo "Generating JWT secret for first run..."
        if [ -n "$$JWT_SECRET" ]; then
          printf "JWT_SECRET=%s\n" "$$JWT_SECRET" > /secrets/secrets.env
        else
          printf "JWT_SECRET=" > /secrets/secrets.env
          cat /dev/urandom | tr -dc a-f0-9 | head -c 64 >> /secrets/secrets.env
          printf "\n" >> /secrets/secrets.env
        fi
      else
        echo "JWT secret already exists, skipping generation."
      fi
      if [ ! -f /secrets/pg_password ]; then
        echo "Generating database password for first run..."
        if [ -n "$$POSTGRES_PASSWORD" ]; then
          printf "%s" "$$POSTGRES_PASSWORD" > /secrets/pg_password
        else
          cat /dev/urandom | tr -dc a-zA-Z0-9 | head -c 32 > /secrets/pg_password
        fi
      else
        echo "Database password already exists, skipping generation."
      fi
      '

  postgres:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-denarius}
      POSTGRES_USER: ${POSTGRES_USER:-denarius}
      POSTGRES_PASSWORD_FILE: /run/secrets/pg_password
    volumes:
      - denarius_db_data:/var/lib/postgresql/data
      - denarius_secrets:/run/secrets:ro
    networks:
      - denarius_net
    depends_on:
      config-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-denarius} -d ${POSTGRES_DB:-denarius}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: denarius-backend:${DENARIUS_VERSION:-1.1.0}
    # These images are built locally and never pushed to a registry. Without
    # this, a redeploy that pulls (Portainer's "re-pull image", compose pull)
    # would try to fetch denarius-backend from Docker Hub and fail.
    pull_policy: build
    restart: unless-stopped
    environment:
      # DATABASE_URL and JWT_SECRET are built/sourced by the container entrypoint.
      # Set them here only if you want to override auto-generation.
      DATABASE_URL: ${DATABASE_URL:-}
      JWT_SECRET: ${JWT_SECRET:-}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-15}
      REFRESH_TOKEN_EXPIRE_DAYS: ${REFRESH_TOKEN_EXPIRE_DAYS:-30}
      ENVIRONMENT: ${ENVIRONMENT:-production}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:9723}
      BACKUP_RETAIN_DAYS: ${BACKUP_RETAIN_DAYS:-30}
      POSTGRES_USER: ${POSTGRES_USER:-denarius}
      POSTGRES_DB: ${POSTGRES_DB:-denarius}
    volumes:
      - ./backups:/app/backups
      - denarius_secrets:/run/secrets:ro
    networks:
      - denarius_net
    depends_on:
      config-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/api/v1/system/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        VITE_API_BASE_URL: /api/v1
    image: denarius-frontend:${DENARIUS_VERSION:-1.1.0}
    pull_policy: build
    restart: unless-stopped
    ports:
      - "${APP_PORT:-9723}:80"
    networks:
      - denarius_net
    depends_on:
      backend:
        condition: service_healthy

  backup-cron:
    build:
      context: ./backup
      dockerfile: Dockerfile
    image: denarius-backup-cron:${DENARIUS_VERSION:-1.1.0}
    pull_policy: build
    restart: unless-stopped
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_DB: ${POSTGRES_DB:-denarius}
      POSTGRES_USER: ${POSTGRES_USER:-denarius}
      # PGPASSWORD is sourced from the secrets volume by the entrypoint if not set here.
      PGPASSWORD: ${POSTGRES_PASSWORD:-}
      BACKUP_RETAIN_DAYS: ${BACKUP_RETAIN_DAYS:-30}
    volumes:
      - ./backups:/backups
      - denarius_secrets:/run/secrets:ro
    networks:
      - denarius_net
    depends_on:
      config-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy

networks:
  denarius_net:
    driver: bridge

volumes:
  denarius_db_data:
  denarius_secrets:
